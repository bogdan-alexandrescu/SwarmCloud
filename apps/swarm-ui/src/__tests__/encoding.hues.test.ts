  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
W  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
§  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
§  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
K  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
X  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
Y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
V  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
§  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
W  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
j  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
8  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
V  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
Y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
V  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
|  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
Y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
V  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
J  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
J  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
!  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
^  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
|  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
&  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
^  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
|  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
^  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
^  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
|  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
|  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
^  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
!  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
!  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
!  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
!  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
q  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
<  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
9  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
7  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
8  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
9  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
7  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
7  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
7  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
z  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
9  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
9  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
9  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
8  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
8  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
8  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
8  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
9  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
8  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
9  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
7  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
z  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
7  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
P  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
Δ  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
F  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
q  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
q  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
X  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
q  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
P  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
V  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
§  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
§  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
P  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
Y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
|  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
|  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
|  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
|  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
8  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
V  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
V  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
W  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
_  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
Y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
P  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
j  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
<  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
P  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
W  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
j  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
8  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
q  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
8  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
!  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
q  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
_  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
_  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
_  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
P  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
K  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
j  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
j  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
<  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
j  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
!  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
j  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
!  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
j  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
q  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
#  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
8  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
7  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
7  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
9  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
#  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
8  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
7  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
§  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
§  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
7  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
P  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
q  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
P  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
<  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
§  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
7  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
P  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
P  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
7  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
F  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
W  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
P  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
#  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
8  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
9  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
7  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
#  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
9  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
#  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
9  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
P  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
F  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
W  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
P  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
V  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
7  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
W  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
7  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
§  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
Y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
<  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
Y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
|  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
!  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
W  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
§  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
7  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
7  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
@  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
F  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
q  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
&  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
&  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
^  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
@  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
Y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
@  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
!  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
9  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
F  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
F  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
X  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
9  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
9  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
j  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
V  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
Y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
P  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
Y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
&  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
&  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
|  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
|  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
&  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
&  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
q  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
F  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
!  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
q  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
z  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
Y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
§  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
7  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
V  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
Y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
Y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
q  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
^  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
|  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
|  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
q  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
8  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
j  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
§  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
°  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
W  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
W  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
W  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
<  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
j  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
j  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
<  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
;  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
j  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
j  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
j  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
W  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
L  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
R  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
Y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
"  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
%  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
W  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
^  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
^  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
*  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
4  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
5  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
H  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
8  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
6  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
§  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
2  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
1  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
j  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
A  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
:  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
+  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
\  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
/  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
W  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
?  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
N  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
!  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
!  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
[  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
0  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
]  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
G  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
q  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
3  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
v  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
m  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
U  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
T  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
C  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
O  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
M  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
S  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
=  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
g  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
>  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
f  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
c  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
W  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
$  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
{  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
k  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
y  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
u  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
l  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
`  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
.  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
B  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
p  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
i  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
n  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
t  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
e  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
(  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
r  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
b  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
x  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
-  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
s  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
h  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
a  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
d  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
o  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
w  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
'  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
,  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
W  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
I  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
D  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
E  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
   // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
}  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
)  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //

  // verdict. The follow-up names two greys, by kind: a mark or a fill (the
  // track, the chip, the dot) takes the primitives' `--text-faint` per CH-17;
  // a WORD (the success box's heading, the legacy `.tag.ok`) takes
  // `--text-dim`, CH-17's word rule ("plain or `--text-dim`", §6.6) and the
  // grey #159 gave the tag. Clarified on #85, 2026-09-25: an earlier fix-up
  // read the follow-up as `--text-faint` for the words too.
  //
  // The heading and the tag are separate `it`s so that each is shown red on
  // its own: the heading would otherwise stop the block before the tag's line
  // ran.
  //
